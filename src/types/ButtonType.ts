import { ReactNode } from "react";


export type ButtonProps = {
    props? : any;
    children?: ReactNode;
    $size? : string;
    $bc? : string;
    $fontcolor? : string;
    $bdc? : string;
  };

